import drag from '../../../src/practice/tools/drag';

describe('drag:手写实现拖拽', () => {
    // 暂无测试用例
    // const cases = [
    //     {
    //         inputs: [],
    //         result: [],
    //     },
    // ];

    // test.each(cases)(`drag:inputs is $inputs,result should be $result`, ({ inputs, result }) => {
    //     expect(drag(...inputs)).toEqual(result)
    // })
});
