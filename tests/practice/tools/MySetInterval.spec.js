import mySetInterval from '../../../src/practice/tools/MySetInterval';

describe('mySetInterval:使用setTimeout模拟setInterval', () => {
    // 暂无测试用例
    // const cases = [
    //     {
    //         inputs: [],
    //         result: [],
    //     },
    // ];

    // test.each(cases)(`mySetInterval:inputs is $inputs,result should be $result`, ({ inputs, result }) => {
    //     expect(mySetInterval(...inputs)).toEqual(result)
    // })
});
