import NavtiveElement from '../../src/practice/NavtiveElement';

describe('navtiveElement:虚拟DOM转化为真实DOM', () => {
    //该用例暂时无法测试

// const child1 = NavtiveElement.createElement({tagName:'button',props:{class:'cancel'},children:['取消']});
// const div = NavtiveElement.createElement({tagName:'div',props:{class:'test'},children:[child1,'测试文本']});
//     test(`navtiveElement:inputs is $inputs,result should be $result`, ({ }) => {
//         // expect(navtiveElement(...inputs)).toEqual(result)
//     })
    test(`占位测试用例`, ({ }) => {
        expect(0).toEqual(0)
    })
});
